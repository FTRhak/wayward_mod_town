var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
define(["require", "exports", "../../util/Define"], function (require, exports, Define_1) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    Define_1 = __importDefault(Define_1);
    Define_1.default(Array.prototype, "spliceWhere", function (predicate) {
        // choose the faster method based on the array length
        if (this.length > 10) {
            let i = 0;
            this.splice(0, Infinity, ...this.filter(v => !predicate(v, i++)));
        }
        else {
            for (let i = 0; i < this.length; i++) {
                if (predicate(this[i], i)) {
                    this.splice(i--, 1);
                }
            }
        }
        return this;
    });
});
